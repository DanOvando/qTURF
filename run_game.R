run_game = function(scene,
                    patches,
                    time,
                    non_coop_cost,
                    game_tol = 1e-3) {

  turfs = unique(scene$turf)

  while (diff > game_tol) {

    for (i in seq_along(turfs)){

    } #close passing

  } #close game loop


}